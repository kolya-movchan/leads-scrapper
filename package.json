{
  "name": "lead-finder",
  "version": "1.0.0",
  "description": "Scheduled lead finder: scrapes Reddit via Apify, filters with Claude, notifies via Telegram",
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "once": "tsx src/index.ts --once",
    "maps": "tsx src/maps.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.110.0",
    "axios": "^1.18.1",
    "dotenv": "^17.4.2",
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "@types/node-cron": "^3.0.11",
    "tsx": "^4.23.0",
    "typescript": "^7.0.2"
  }
}
